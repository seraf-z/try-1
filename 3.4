/*
4
Написать функцию, которая возвращает сумму указанных чисел и всех
чисел между ними. Шаг равен 1.
Первое число необязательно наименьшее
function sumAll(arr) {
  // your code
}

*/

function sumAll2(n3, n4) {
  let min = Math.min(n3, n4);
  let max = Math.max(n3, n4);
  let sum = ( min + max ) * ( max - min + 1 ) / 2;
  return sum
}
