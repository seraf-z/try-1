/*3112
Написать функцию, удаляющую элемент из массива по индексу
var arr = [1, 2, 3, 4, 5];
function drop(arr, i) {
    // ...
}
drop(arr, 3) // [1, 2, 3, 5]
*/

function drop(a, b) {
  let clone = [].concat(a);
  
  clone.splice(b, 1);
  
  return clone;
}
